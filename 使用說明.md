# 🏎️ JetBot 期末專案：道路跟隨 + 交通路牌辨識 合併部署使用說明書

本說明書引導您將 **Project 5 (道路跟隨)** 與 **Project 6 (路牌辨識)** 的模型與程式碼合併，並成功部署至 **JetBot (Jetson Nano)** 自走車上運行。

---

## 📁 1. 準備工作與檔案傳輸清單

在開始之前，請使用 **WinSCP**、**FileZilla** 或 `scp` 指令將以下 PC 端的檔案上傳到 JetBot 上。

> ⚠️ **重要提示**：JetBot 端的部署根目錄建議設為 `~/trt_yolov4-tiny-master/`，這是 Jetson Nano 上預裝 YOLOv4-tiny 推論模組的工作目錄。

### 傳輸檔案對照表：

| 來源檔案 (PC 端路徑) | 目標放置路徑 (JetBot 端) | 說明 |
| :--- | :--- | :--- |
| `Final.ipynb` | `~/trt_yolov4-tiny-master/Final.ipynb` | **合併控制主程式** |
| `Project5_road_detect/best_steering_model_xy_trt.pth` | `~/trt_yolov4-tiny-master/Project5_road_detect/best_steering_model_xy_trt.pth` | 道路跟隨 TensorRT 模型 |
| `Project6_sign_detect/jetbot_deploy/yolov4-tiny-416.weights` | `~/trt_yolov4-tiny-master/yolo/yolov4-tiny-416.weights` | 路牌辨識權重檔 |
| `Project6_sign_detect/jetbot_deploy/yolov4-tiny-416.cfg` | `~/trt_yolov4-tiny-master/yolo/yolov4-tiny-416.cfg` | 路牌辨識架構設定檔 |
| `Project6_sign_detect/jetbot_deploy/obj.names` | `~/trt_yolov4-tiny-master/yolo/obj.names` | 號誌類別名稱檔 |

---

## 🛠️ 2. 模型轉換與編譯 (於 JetBot 終端機執行)

為了讓 JetBot 達到即時推論速度（> 10 FPS），必須在車端將 YOLO 模型編譯為 TensorRT 引擎。

1. **開啟終端機並連線至 JetBot** (SSH 登入)。
2. **切換到 `yolo` 編譯目錄**：
   ```bash
   cd ~/trt_yolov4-tiny-master/yolo/
   ```
3. **執行 ONNX 格式轉換**：
   ```bash
   python3 yolo_to_onnx.py -c 4 -m yolov4-tiny-416
   ```
   *註：`-c 4` 代表類別數量為 4 類。*
4. **執行 TensorRT 引擎編譯 (啟用 FP16 半精度加速)**：
   ```bash
   python3 onnx_to_tensorrt.py -c 4 -m yolov4-tiny-416
   ```
   *編譯約需 2-5 分鐘，完成後會在同目錄生成 `yolov4-tiny-416.engine` 檔案。*

---

## 🚗 3. 運行主程式 (`Final.ipynb`)

開啟瀏覽器登入 JetBot 的 **Jupyter Notebook** 網頁介面，進入工作目錄並點擊開啟 **`Final.ipynb`**。本程式共分為三個單元，您可以分步測試或直接進行最終展示：

### 🏁 單元一：純道路跟隨測試 (調校 PID 參數)
* **目的**：只載入 ResNet-18 道路循跡模型與 224x224 相機，不受 YOLO 與 PyCUDA 干擾，便於單獨調校 PID 與轉向參數。
* **執行步驟**：
  1. 依序執行 **`A1` ~ `A4`** 單元格載入模型、設定滑桿。
  2. 執行 **`A6`** 啟動自動駕駛，將車輛放上賽道進行測試與調參。
  3. **調參說明**：
     * **`Speed Gain` (速度)**：預設 `0.18`。
     * **`P Gain` (比例項)**：預設 `0.08`，若修正太慢可調大；若車頭劇烈搖晃可微幅調小。
     * **`D Gain` (微分項)**：預設 `0.00`，若車頭產生左右 S 型擺盪，可嘗試調至 `0.02` ~ `0.05`。
     * **`Motor Bias` (偏置)**：若左右輪有硬體偏差導致直行偏斜，可藉此滑桿進行微調。
  4. **切換單元前必做**：執行 **`A7. 安全停止單元一`** 單元格釋放相機與馬達資源。

### 🚥 單元二：純交通路牌辨識測試
* **目的**：只載入 YOLOv4-tiny 偵測模型與 416x416 相機，測試號誌動作。自走車在此模式下只會以直線前進（不進行轉向修正），偵測到路標時執行對應的停走動作。
* **執行步驟**：
  1. 依序執行 **`B1` ~ `B4`** 單元格。
  2. 執行 **`B6`** 啟動號誌偵測，手持或放置號誌卡片在鏡頭前測試。
  3. **切換單元前必做**：執行 **`B7. 安全停止單元二`** 單元格釋放相機資源。

### 🏁🏁 單元三：合併最終版 (道路跟隨 + 路牌辨識)
* **目的**：雙模型並行推論。使用 416x416 相機流，推論 YOLO 路牌後，對影像進行即時下採樣 (224x224) 供 ResNet 進行轉向預測，為期末考評分的最終整合版本。
* **執行步驟**：
  1. 依序執行 **`C1` ~ `C4`** 單元格。
  2. 執行 **`C6`** 啟動完整自動駕駛，自走車將會同時進行道路跟隨與路標反應。
  3. **切換/結束前必做**：執行 **`C7. 安全停止單元三`** 停止自走車並釋放系統資源。

---

## 🚥 4. 號誌控制動作說明 (採用 `yolo26` 類別順序)

本專案完美契合期末簡報規定的行為指標：

| Class ID | 號誌名稱 | 影像畫面標記 | 觸發寬度門檻 | JetBot 對應動作 |
| :---: | :---: | :---: | :---: | :--- |
| **Class 3** | `stop` | `ID:3` (綠色標籤) | $\ge$ `Alert Width` | **原地停止 2 秒** 後，繼續跟隨道路行駛 (防重複冷卻：10秒)。 |
| **Class 2** | `rail` | `ID:2` (黃色標籤) | $\ge$ `Alert Width` | **原地停止 5 秒** 後，繼續跟隨道路行駛 (防重複冷卻：10秒)。 |
| **Class 1** | `pedestrian` | `ID:1` (青色標籤) | $\ge$ `Alert Width` | **減速行駛**（基礎速度自動下調為 0.7 倍），通過後自動恢復。 |
| **Class 0** | `blocked` | `ID:0` (紅色標籤) | $\ge$ `Alert Width` | **永久停止行駛**，自動解除相機監聽並安全結束程式。 |

### 🛡️ 防誤判與防假路標機制 (拿滿 10% 分數關鍵)：
* **寬度過濾**：小於 `Alert Width` 的路牌（代表距離很遠，或是賽道旁的假路標貼紙）會被程式自動忽略，避免提早停車。
* **高信心度過濾**：推論信心度設為 `0.6`，只對高可信度的路牌卡片做出反應。
* **重複觸發保護 (Cooldown)**：停止動作完成後會開啟 10 秒冷卻，避免車子在尚未完全駛離該路牌前，又被相機重複偵測而卡在原地。

---

## 🚨 5. 安全緊急停機與資源釋放守則

由於 Jetson Nano 對 GStreamer 相機資源有嚴格的單例限制，若未釋放即重新開啟會造成 **Resource busy** 或相機死鎖。
* ⚠️ **黃金守則**：每當您要從單元一、二或三切換至其他部分時，**必須執行該單元最下方的安全停止單元格**（例如 `A7`、`B7` 或 `C7`）。
* 如果相機已經卡死（預覽畫面全黑或出現報錯），請在 JetBot 終端機執行以下命令重啟相機守護行程：
  ```bash
  sudo systemctl restart nvargus-daemon
  ```

